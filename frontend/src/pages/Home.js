import { Fragment } from 'react';
import NotesList from '../components/notes/NotesList';

const Home = () => {
  return (
    <Fragment>
      <NotesList />
    </Fragment>
  );
};

export default Home;
